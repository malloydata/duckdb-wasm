{
    "repository": {
        "type": "git",
        "url": "https://github.com/duckdb/duckdb-wasm.git"
    },
    "private": true,
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "lint:fix": "yarn workspace @malloydata/duckdb-wasm lint --fix && yarn workspace @malloydata/benchmarks lint --fix && yarn workspace @malloydata/duckdb-wasm-shell lint --fix",
        "docs": "yarn workspace @malloydata/duckdb-wasm docs",
        "benchmarks:build:browser": "yarn workspace @malloydata/benchmarks build:browser",
        "benchmarks:build:node": "yarn workspace @malloydata/benchmarks build:node",
        "benchmarks:build": "yarn workspace @malloydata/benchmarks build"
    },
    "devDependencies": {
        "badge-maker": "^3.3.1"
    }
}
